token = "OTI0MzU0ODYwNTUwMzQwNjYx.YcdWjA.wqWJpqqWvN3Dd4rM8hhSjzRIcBQ"
prefix = "."

topgg = "x"
topgg_pass = "x"

tips_earnings = 500
exp_per_level = 500
items_per_page = 4

daily_cooldown = 86400 # 1 day
company_cooldown = 604800 # 7 days
tasks_reload = 21600

clean_grace = 259200 # 3 days
clean_time = 86400 # 1 day

report_channel = 0000000000000000
no_cooldown = [0000000000000000, 0000000000000000, 0000000000000000, 0000000000000000]
admins = [0000000000000000, 0000000000000000]
event_chance = 5

whitelisted_cmd = ["ping", "found", "help", "tutorial", "botinfo", "ban"]

help_embed = """
🎓 **Getting Started** 🎓

◽ To get started with your pizzeria type `{p}profile` / `{p}p` to see your pizzerias info.
◽ Start by naming your pizzeria using `{p}name` / `{p}n`
◽ Give it a nice slogan with `{p}slogan` / `{p}s`


🛠 **Maintenance** 🛠

◽ But hold on! Use `{p}recipe` /` {p}r` to access your cookbook.
◽ Use `{p}ingredients` / `{p}i` to buy the ingredients you need for your pizzeria.
◽ Use `{p}storage` / `{p}st` to view all of your ingredients in your storage. **SPACE IS LIMITED**.
◽ Use `{p}clean` / `{p}c` to clean your pizzeria **daily**, or else your **happiness will go down**.

 ~ *Now you are ready to earn.* ~


💵 **Earning** 💵

◽Start baking pizzas using `{p}work` / `{p}w`
◽ You can also use `{p}tips` / `{p}t` to earn some bonus money.

**Work**
◽ `{p}work`: Type the ingredients needed in the order to complete it. (eg. dough, tomato, mozz, pep)
    
**Tips**
◽ `{p}tips`: Unscramble one of the words provided in order to complete it.


🧰 **EMPLOYEES** 🧰‍
    
*Now that your shop is flourishing it's time to hire some employees to do help you out.*
    
◽ Use `{p}hire` / `{p}h` to pick the right employee for your pizzeria
◽ Use `{p}employee` / `{p}e` to view all your employees in your pizzeria
◽ Use `{p}fire` / `{p}f` to fire any one of your employees
◽ Use `{p}jobs` / `{p}j` to view the jobs in your pizzeria
◽ Use `{p}unassign` /`{p}u` to unassign an employee from their current position.

❓ **HINT:** *You should assign employees to jobs they have the skills for. Make sure to check your employee's stats, before assigning them to get the best out of them*


🆙 **UPGRADES** 🆙

◽ Use `{p}skills` to check your stats. You get a skill point per level you earn.
◽ Wanna upgrade your shop? Use `{p}upgrade` / `{p}u`  to see what more you can add to your little pizzeria
◽ There are also a few bonuses you can use to boost your shop using `{p}shop` /`{p}s`
◽ Got bored of the beach? Type `{p}location` / `{p}l`  to check out other locations you can move your business to.
◽ Add loads of new modules to your shop using `{p}modules` / `{p}m`.
    

💸 **FREE MONEY** 💸

◽ Don't forget to `{p}daily` /  `{p}d` for a **daily bonus**!
◽ Try your luck using `{p}roll` and `{p}coinflip` and win some petty cash for your needs.
◽ You can also play the slots using `{p}/slots`.
◽ You can also use `{p}beg` to beg in the streets.
◽ You can also use `{p}search` to search around for items or money.
◽ You can fish for ingredients using `{p}fish`, hunt using `{p}hunt` and others in `{p}skills`.

:convenience_store: **SHOP** :convenience_store:

:white_medium_small_square: Head over to our shop using `{p}shop`/ `{p}s` to buy some fun items and boosts.
:white_medium_small_square: Bought items will be stored in your inventory, you can use `{p}inventory` to view your inventory
:white_medium_small_square: Buy our items using `{p}buy` / `{p}b`.
:white_medium_small_square: Have too much crap or stuff you don't need? Use `{p}sell` and sell it all!
:white_medium_small_square: Use you purchase using `{p}use` / `{p}u`.


🏧 **Banking** 🏧

◽ Start by selecting a bank using `{p}bank` / `{p}b` . It'll show you a list of our available banks.
◽ Deposit your money using `{p}deposit` / `{p}d`. Watchout for those interest rates! They'll generate you passive income.
◽ Help a friend out by using `{p}transfer` / `{p}t` and transfer some green in your friends pockets.
◽ Don't forget to claim your interest using `{p}claim` / `{p}c`.


⚙ **Support** ⚙

◽ If you encounter any bugs use `{p}report` to report any bugs or general suggestions you have.
◽ If the bot isn't responding please use `{p}ping` to ping the bot.
◽ If you still have questions checkout the server or if you prefer the human touch feel free to tag our mods.""".format(p=prefix)
